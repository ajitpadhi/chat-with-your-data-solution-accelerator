# Worklog — 2026-07-03

## Focus

BUG-0090 (admin 401 / user_id header handling) — review, finding remediation, and closure.

## Done

- **Reviewed the BUG-0090 implementation** (Task Reviewer): 6 per-phase RPI validations, all PASS; independently re-ran the full suite (2177 passed / 1 skipped), bicep build exit 0, all shared gates green. 0 Critical / 0 Major findings. Review log: `reviews/2026-07-02/bug-0090-admin-user-id-plan-review.md`.
- **Resolved the review findings:**
  - **F-1 (stale docs)** — refreshed every current-state / live-status admin-auth reference to the header-GUID / ingress-enforced posture: `mvp_status.md` (exec snapshot, §1, §3 + diagram, the whole §4 auth-flow section + flowchart + gaps + completion-plan A2), `admin_runtime_config.md` (§1.7, §2 intro, 5 per-route status-code lines, `updated_by`), `project_status.md` (2 status rows). Left the historical records (dev-plan §0.1 ledger, `bugs.md` bug history, ADRs 0023/0024, executed `frontend-user-identity-plan.md`) and frontend client-side UX claims unchanged.
  - **F-2 (cosmetic)** — restored the merged allow-list docstring bullets in `test_no_anonymous_dict_returns.py`; gate green.
  - **F-3** — no action (per the finding's own guidance; implementation complete).
- **Marked BUG-0090 fixed** (2026-07-03) in `bugs.md`. The code fix is complete and all local gates are green; the live deploy + `GET /api/admin/status` 200 check is the operator's next step (Hard Rule #10 — deploy is gated).

## Decisions

- Trusting the client-forgeable `x-ms-client-principal-id` header is accepted by design (matches MACAE); real auth, when required, is enforced at the ingress/proxy. The durable admin-write protection (WI-01) is ingress-level, tracked as a follow-up — close it before exposing the backend publicly.

## Next steps / open

- **BUG-0090 live deploy** — `azd provision` / `azd deploy backend` from a `v2`-cwd terminal; verify `/api/admin/status` → 200 for a GUID header and the default GUID; this also regenerates the untracked `main.json`.
- **BUG-0082 (open, backend/medium)** — backend Container App crash-loops when Postgres is unreachable (no bounded connect timeout in the lifespan). Not exercised by the current cosmosdb env. Fix direction: bounded asyncpg connect timeout + fail-fast lifespan + `PYTHONUNBUFFERED=1`.
- Follow-ups from the review (non-blocking): WI-01 ingress protection, WI-04 `#35x` docstring scrub in `admin.py`, WI-06 ADR 0030 index row, WI-07 BUG-0090 area reclassification.

---

## Postgres v2 deployment to `<RESOURCE_GROUP>` (afternoon session)

### Focus

Deploy CWYD v2 in **postgresql** mode into a fresh resource group, fixing the infra blockers hit along the way.

### Done

- **BUG-0082 fixed** (backend/medium) — `PostgresClient` now passes a bounded `timeout=_POOL_CONNECT_TIMEOUT_SECONDS` (10.0 s) to `asyncpg.create_pool(...)`, so an unreachable Postgres fails fast at lifespan startup instead of crash-looping on the default unbounded connect. Unit test `test_create_pool_uses_bounded_connect_timeout_and_fails_fast` green; full suite (56 targeted) green, `pyright --strict` 0/0/0.
- **First `azd up` (eastus2) hit two blockers:**
  - Postgres Flexible Server is **offer-restricted in eastus2** for this subscription (`LocationIsOfferRestricted`). Decision: redeploy in **uksouth** (AI quota pre-verified — gpt-5.1 GlobalStandard and text-embedding-3-large Standard both have ample free quota in uksouth).
  - **BUG-0061 recurred** on the fresh provision — the shipped `dependsOn`-based Event Grid back-port was insufficient.
- **BUG-0061 investigated (vs. MACAE) and durably fixed** — see below.
- **Cleaned up the partial eastus2 deployment** — deleted + purged the three cognitive accounts (`spch-`/`cs-`/`aisa-<SUFFIX>`, the last after deleting its nested Foundry project) so the names are free for uksouth, and deleted `<RESOURCE_GROUP>` (background).

### BUG-0061 — durable Event Grid MI fix

- **Root cause (confirmed):** RBAC replication latency, not creation ordering. `dependsOn` orders resource *creation*, but Azure RBAC is eventually consistent, and the **system-assigned** topic MI does not exist until the AVM `system-topic` module completes — so the `Storage Queue Data Message Sender` grant was authored moments before Event Grid's **synchronous** delivery-authorization preflight, with ~0 s replication lead → 401.
- **MACAE comparison:** MACAE uses no Event Grid at all; it handles grant-then-use via idempotent azd `postprovision` `az` scripts (post-ARM, replication settled) + a documented replication-delay retry. No pattern to copy directly.
- **Fix (durable, mirrors the existing-topic reuse path):** deliver as the **shared UAMI** (`id-<SUFFIX>`, exists at t=0) instead of the topic's system MI. Three coordinated edits in [../infra/main.bicep](../infra/main.bicep): (a) AVM `system-topic` `managedIdentities.userAssignedResourceIds: [ userAssignedIdentity.outputs.resourceId ]`; (b) `eventGridQueueSenderRole` grants the role to the UAMI on the storage account with **no** dependency on the topic, so ARM creates it at the start of the deployment and it replicates through the multi-minute provisioning tail before the preflight runs; (c) `blobCreatedSubscription.deliveryWithResourceIdentity.identity` → `UserAssigned`. `az bicep build` exit 0 (only pre-existing BCP081/BCP334 warnings elsewhere).
- **Rejected alternatives:** `deploymentScripts` grant-then-wait (needs `allowSharedKeyAccess=true` backing storage — conflicts with ADR 0002); moving the subscription into a `postprovision` hook (kept as a backstop, unneeded once the grant is UAMI-based and topic-independent).

### Review remediation (task-review follow-up)

Ran a task-review of the landed work, then addressed its findings:

- **F-1 (Major, env leak):** this worklog had embedded the literal resource-group name (which also contains the azd env name), turning `test_no_env_specific_content` red. Swapped to the `<RESOURCE_GROUP>` placeholder; gate green.
- **F-2 (Minor, doc hygiene):** aligned the stale reuse-path comment in [../infra/main.bicep](../infra/main.bicep) to the account-scope rationale used by both role grants (and fixed an un-rendered Unicode escape literal — an em-dash left as six literal ASCII characters — to a plain ASCII `--`).
- **F-3 (Minor, transparency):** added a note to the BUG-0082 row in [bugs.md](bugs.md) recording the 2026-07-03 code fix + regression test (`PYTHONUNBUFFERED=1` already set in `Dockerfile.backend`); status left `open` pending the live repro.
- **F-4 (Minor, hygiene):** gitignored `.copilot-tracking/` so tooling-written tracking notes (which carry env-specific IDs) can never be `git add`-ed by accident.
- **F-5 (Observation):** deferred — the reuse path does not assign the UAMI to an *existing* v1 topic, only reachable when `existingEventGridTopicName` is set (not this single-topic deployment).

### Next steps / open

- **uksouth redeploy** — `azd env set AZURE_LOCATION uksouth` (+ `AZURE_ENV_AI_SERVICE_LOCATION uksouth`), pre-create `<RESOURCE_GROUP>` in uksouth, then bare `azd up` in **sync** mode from a `v2`-cwd terminal. Verify Postgres provisions and BUG-0061 does not recur.
- **Follow-up (F-5, not part of this fix):** verify the `useExistingEventGridTopic` reuse path assigns the UAMI to the *existing* v1 topic (the `existing` topic reference does not set its identity) — potential latent gap when reusing a v1 topic.

### uksouth deploy — completed

- **Region switched + RG pre-created**, then `azd up` (bare, sync, from a `v2`-cwd terminal). **Provision succeeded** — every resource `Succeeded`, including the PostgreSQL Flexible Server and the Event Grid system topic.
- **BUG-0061 non-recurrence CONFIRMED ✅** — the Event Grid system-topic subscription `blob-created-to-doc-processing` provisioned `Succeeded` with **no 401**. A delivery-identity role-grant 401 (the original BUG-0061 failure) would have failed the subscription and the whole deployment; both succeeded. The durable UAMI-delivery Bicep through-line works on a fresh single-topic provision.
- **App deploy was initially incomplete — root cause: local Docker daemon was down.** The first `azd up` finished provision but its deploy phase stalled in image packaging/push and exited without rolling the Container Apps off the placeholder image (`mcr.microsoft.com/k8se/quickstart:latest`), leaving ACR empty and the backend StartUp probe failing by design.
- **Fix:** started Docker Desktop (daemon reachable), re-ran `azd deploy` from the `v2` cwd → **SUCCESS**. All three Container Apps (`backend`, `frontend`, `function`) now run the real `cr<SUFFIX>.azurecr.io/chat-with-your-data-v2/*` images (tag `azd-deploy-<ts>`); backend `runningStatus: Running`; the six sample PDFs seeded.
- **`/api/health` → `pass` ✅** — `GET .../api/health` returns HTTP 200 with `status: pass`: `foundry_iq` **pass**, `database` **pass** (`db_type=postgresql` — the deployed backend connects to the Flexible Server; the BUG-0082 bounded-timeout path is live and healthy), `search` **skip** (`index_store=pgvector`). Lesson: a "Succeeded" ARM deployment means **provision** succeeded — validate the **app** deploy separately via ACR repositories + the Container App image, not the ARM state alone; and `azd deploy` needs the local Docker daemon reachable even though it uses ACR remote build.

### BUG-0082 live repro -- CONFIRMED

- **Stopped** the Flexible Server -> the backend crash-looped on a **bounded** cadence. The live lifespan traceback (console logs, DB down) walks `_lifespan` (app.py:128 `get_runtime_config`) -> `_ensure_pool` -> `asyncpg.create_pool(...)` -> asyncpg `compat.timeout(timeout)` -> **`TimeoutError`** -> `ERROR: Application startup failed. Exiting.` -- i.e. the bounded 10 s `_POOL_CONNECT_TIMEOUT_SECONDS` fires and the app fails fast + exits cleanly instead of hanging unbounded. Revision state: `CrashLoopBackOff`, restarts=5.
- **Started** the server -> the backend **recovered automatically** on the next cold-start: `/api/health` -> HTTP 200 `status: pass` (`database: pass`, db_type=postgresql) in ~18 s. No manual restart needed.
- **Closed `BUG-0082`** (open -> fixed) in [bugs.md](bugs.md) with the confirmed-repro note. Fix validated end-to-end.

### F-5 fixed — reuse-path Event Grid topic now carries the UAMI

- **Closed the last review finding the right way.** F-5 was a latent gap on the `useExistingEventGridTopic` reuse path: the reuse subscription delivers via `deliveryWithResourceIdentity` pointing at v2's UAMI, but the topic was referenced as `existing` (read-only) and so never assigned that identity — Event Grid can only deliver as an identity the **topic** carries, so a v1-topic-reuse deployment would have failed EG's synchronous delivery preflight.
- **Fix:** converted `existingEventGridTopic` in [../infra/main.bicep](../infra/main.bicep) from an `existing` reference to a **managed** `Microsoft.EventGrid/systemTopics` resource — an idempotent in-place PUT that leaves the immutable `source`/`topicType` unchanged (`source == effectiveStorageResourceId`, the reused storage) and adds the UAMI via a compile-time-resolvable `resourceId('Microsoft.ManagedIdentity/userAssignedIdentities', identityName)` key (a module runtime output can't be an identity dictionary key — BCP120). ARM updates v1's topic in place, attaching the UAMI, so the reuse subscription's delivery identity resolves. Azure allows only one system topic per storage source, so re-declaring the same topic (same name + source) is a valid update, not a create-conflict.
- **Guard:** added `test_reuse_path_topic_carries_uami_identity` to [test_main_bicep.py](../../tests/infra/test_main_bicep.py) pinning that the reuse topic is managed (not `existing`) and carries the UAMI. `az bicep build` exit 0; `pytest tests/infra/test_main_bicep.py` → 40 passed.

### Next steps / open

- None for this task -- the postgresql-mode deployment is live and healthy, BUG-0061 + BUG-0082 are both confirmed fixed, and **all review findings (F-1 through F-5) are addressed** (F-5's reuse-path UAMI-on-topic fix is unvalidatable without an actual v1-topic-reuse deployment, but is compile-verified and guarded).
